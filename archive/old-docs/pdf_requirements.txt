# Requirements for PDF Generation
# Install with: pip install -r pdf_requirements.txt

# Option 1: WeasyPrint (Recommended if Pandoc not available)
markdown>=3.4.0
weasyprint>=60.0

# Option 2: Alternative libraries (optional)
mdpdf>=0.1.0
markdown2pdf>=0.1.0
